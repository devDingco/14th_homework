// script/formHandler.js (stub)
(function (w) {
  "use strict";
  // 폼 초기화/바인딩이 아직 없어도 에러 없이 지나가도록 스텁
  w.initDiaryForm = w.initDiaryForm || function () {
    // 나중에 실제 구현 채우면 됨
    console.log("initDiaryForm: stub loaded");
  };
})(window);
