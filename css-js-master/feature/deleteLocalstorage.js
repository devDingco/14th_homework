const deleteLocalStorage = () => {
    // if (localStorage.getItem("일기콘텐츠")) {
    //     localStorage.removeItem("일기콘텐츠")
    //     alert("로컬스토리지가 비워졌습니다.")
    // } else {
    //     alert("로컬스토리지가 이미 비워져있습니다.")
    // }
    
    // delete 버튼에 id#number 주고, 그 nubmer 로 인덱스 찾아서 로컬스토리지에서 지우기
    const diaryContentArr = document.getElementsByClassName("diary_content")
    // for (let i=0; i < test.length; i++) {
    //     console.log(diaryContentArr[i].id.slice(diaryContentArr[i].id.lastIndexOf("#")+1))
    // }
}